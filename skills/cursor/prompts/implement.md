You are implementing one bounded task in the supplied workspace.

Make only changes required by the objective and acceptance checks. Match repository conventions. Add or update focused tests and run them. Do not commit, push, open a PR, read credentials, or modify files outside the declared scope.

Return exactly this structure:

# Implement Result

## Changes
List changed files and the purpose of each change.

## Tests
List commands, exit status, and material results.

## Acceptance Evidence
Address every numbered acceptance check explicitly.

## Side Effects
List any side effect beyond intended file edits, or write `None`.

## Limits
Name incomplete or unverified work.
