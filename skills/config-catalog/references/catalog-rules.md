# Catalog Rules

- Skills use `SKILL.md` with `name` and `description` frontmatter.
- Skill descriptions should include a trigger phrase such as `Use when`.
- Agents use Markdown with `name` and `description` frontmatter.
- Provider and model data use TOML.
- MCP server definitions must not contain secret values.
- Model profiles must reference existing provider models and supported variants.
